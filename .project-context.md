# Project Context: Flet Token-Driven Design System

This file serves as a memory hook and context handoff for future opencode sessions or developers working in this repository.

## 🚀 Project Overview
A production-ready, fully token-driven **Design System** built with **Flet** and **Python**, packaged using **Poetry**. It features a central JSON token schema, light/dark mode semantic mapping, dynamic component theme-swapping, and a complete interactive documentation web application.

---

## 📁 Directory Structure
```text
design/
├── pyproject.toml              # Poetry configuration and dependency declarations
├── poetry.lock                 # Locked dependency file
├── tests/
│   └── test_design_system.py   # Unit tests for token resolution and component creation
└── src/
    └── design_system/
        ├── __init__.py
        ├── main.py             # Interactive Documentation and Component Sandbox (runs on port 8550)
        ├── tokens/
        │   ├── __init__.py
        │   ├── tokens.json     # Single source of truth for global primitive & semantic tokens
        │   └── manager.py      # TokenManager: python-class to resolve colors & scale scales
        └── components/
            ├── __init__.py
            ├── typography.py   # Scalable typography blocks (Display, Heading, Body, Caption, Monospace)
            ├── button.py       # Custom styled interactive and stateful buttons
            ├── card.py         # Modern container cards (Elevated, Filled, Outline, Interactive Hover)
            ├── input.py        # Token-aware standard textfields (Focus states, password reveal)
            ├── toggle.py       # Styled binary selection components (Switches and Checkboxes)
            └── layout.py       # Standardized spacing and section layout blocks
```

---

## 🛠️ Architecture and Decisions

### 1. Three-Layer Token Scheme (`tokens.json` & `manager.py`)
Following industry standards, design tokens are grouped into:
*   **Primitives (Global)**: Raw color hex codes, layout spacing in pixels, border radius, and typography scales.
*   **Semantic**: Purpose-oriented tags (e.g., `primary`, `background`, `surface`, `text-primary`) mapped separately for `light` and `dark` mode blocks inside `tokens.json`.
*   **Component**: Component-specific definitions referencing semantic scales.

The `TokenManager` loads this JSON and exposes simple helper methods (`get_color(name, dark=False)`, `get_spacing(name)`, `get_radius(name)`, `get_font_size(name)`) resolving all active tokens in O(1) time.

### 2. WSL & Headless Compatibility
Because of missing media and desktop graphics libraries inside headless environments (like standard WSL or Docker VMs), Flet is configured to compile and execute in **Web Browser Mode** rather than a native desktop window:
```python
ft.app(target=main, view=ft.AppView.WEB_BROWSER, port=8550)
```
This serves a highly performant, lightweight local web application on **`http://localhost:8550`**.

### 3. Flet API Refinements and Bug-Fixes
*   **No Container Cursors**: In Flet 0.23, `ft.Container` does not support constructor parameters like `cursor` or `mouse_cursor`. Doing so triggers a runtime `TypeError`. Since Flet automatically displays a pointer hand when an `on_click` event is bound to a container, all hardcoded cursor definitions were removed.
*   **No Direct Text Letter-Spacing**: `letter_spacing` cannot be passed directly into the `ft.Text` initializer in this version. This parameter was removed to maintain native, robust cross-platform text layout.

---

## 🏃 How to Run & Verify

### Start the Design System Web Showcase
```bash
poetry run python3 src/design_system/main.py
```
*   **Active Port**: `8550`
*   **Live App URL**: `http://localhost:8550`

### Run Unit Tests
```bash
poetry run pytest
```
Tests cover token resolution (colors, spacing, radiuses, typography) and component instantiation.
